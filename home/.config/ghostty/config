# --- Appearance ---
theme = Atom One Dark
background = #282c34
foreground = #abb2bf

# --- Font ---
# font-family = "JetBrainsMono Nerd Font Mono"
font-family = "CommitMono Nerd Font"
font-size = 14
# Enable ligatures (Contextual Alternates)
font-feature = calt

# --- Window ---
# Minimal macOS look (native tabs, no heavy borders)
window-padding-x = 5
# window-padding-y = 15
window-decoration = true
macos-titlebar-style = tabs

# --- Behavior ---
# Copy to clipboard on select (optional, but handy)
copy-on-select = true
